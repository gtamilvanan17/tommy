import React from 'react';
import RealEstateCard from 'src/components/RealEstateCard'; // Adjust the import path if needed

const Cards = () => {
  return (
    <div>
      {/* Other existing cards or layout */}
      <RealEstateCard />
      {/* More cards if needed */}
    </div>
  );
};

export default Cards;